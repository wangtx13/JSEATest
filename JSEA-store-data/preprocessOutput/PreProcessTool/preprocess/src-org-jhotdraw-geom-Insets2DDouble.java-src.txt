insets2 d 1 1 2006 07 08 1996 2006 disclose accordance geom geom xml insets2 d author 2006 07 08 dom storable <br>1 0 january 14 2006 created insets2 d cloneable io serializable dom storable inset top value top rectangle yield location top serial see #clone top inset left value left rectangle yield location left edge serial see #clone left inset bottom value subtracted bottom rectangle yield location bottom serial see #clone bottom inset right value subtracted right rectangle yield location right edge serial see #clone right creates initializes <code> insets2 d double< code> 0 insets <p> constructor dom storable insets2 d creates initializes <code> insets2 d double< code> specified top left bottom right insets top inset top left inset left bottom inset bottom right inset right insets2 d top left bottom right top = top left = left bottom = bottom right = right checks whether two insets are equal two instances <code> insets2 d double< code> are equal four values fields <code>top< code> <code>left< code> <code>bottom< code> <code>right< code> are equal <code>true< code> two insets are equal otherwise <code>false< code> since j d k1 1 equals obj obj insets2 d insets2 d insets = insets2 d obj top == insets top left == insets left bottom == insets bottom right == insets right hash insets2 d hash insets2 d hash sum1 = left + bottom sum2 = right + top val1 = sum1 sum1 + 1 2 + left val2 = sum2 sum2 + 1 2 + top sum3 = val1 + val2 bits sum3 sum3 + 1 2 + val2 representation <code> insets2 d double< code> intended used debugging purposes content format vary between implementations empty <code>null< code> representation <code> insets2 d double< code> get get + top= + top + left= + left + bottom= + bottom + right= + right + create copy copy <code> insets2 d double< code> clone clone clone supported e shouldn t happen since we are cloneable internal initialize j n i field ids init ids write dom output out out add attribute top top 0d out add attribute left left 0d out add attribute bottom bottom 0d out add attribute right right 0d read dom input top = get attribute top 0d left = get attribute left 0d bottom = get attribute bottom 0d right = get attribute right 0d 