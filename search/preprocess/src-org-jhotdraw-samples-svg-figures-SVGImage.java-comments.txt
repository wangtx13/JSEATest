/* * @ # s v g image 1 0 july 8 2006 * * copyright c 1996 2006 by the original authors of * and all its contributors * all rights reserved * * software is the confidential and proprietary information of * confidential information you shall not disclose * such confidential information and shall use it only in accordance * with the terms of the license agreement you entered into with * */package samples svg figures * geom * draw * samples svg * /** * s v g image * <p> * f i x m e implement me * * @author werner randelshofer * @version 1 0 july 8 2006 created */public s v g image attributed figure s v g figure { rectangle2 d bounds = rectangle2 d /** creates a instance */ s v g image { s v g set defaults } draw fill graphics2 d g { g fill bounds } draw stroke graphics2 d g { g draw bounds } basic transform affine transform ty { } basic set bounds point2 d start point2 d end { } rectangle2 d get bounds { rectangle2 d bounds clone } get restore data { get bounds } restore to restore data { bounds = rectangle2 d rectangle2 d restore data clone } contains point2 d p { bounds contains p } s v g image clone { s v g image that = s v g image clone that bounds = rectangle2 d bounds clone that } is empty { }} 