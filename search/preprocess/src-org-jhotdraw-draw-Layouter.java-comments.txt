/* * @ # layouter 2 0 2006 01 14 * * copyright c 1996 2006 by the original authors of * and all its contributors * all rights reserved * * software is the confidential and proprietary information of * confidential information you shall not disclose * such confidential information and shall use it only in accordance * with the terms of the license agreement you entered into with * ??? */package draw * geom * /** * a layouter encapsulates a algorithm to layout * a composite figure it is passed on to a figure which delegates the * layout task to the layouter's layout * the layouter might need access to some information * specific to a certain figure in order to layout it out properly * * note currently only the graphical composite figure uses * such a layouter to layout its child components * * * @author werner randelshofer * @version 2 0 2006 01 14 changed to support precision coordinates * <br>1 0 2003 12 01 derived from 5 4b1 */public layouter /*extends serializable storable*/ { /** * calculate the layout the figure and all its subelements the * layout is not actually performed but just its dimensions are calculated * * @param anchor start point the layout * @param minimum point the layout */ rectangle2 d calculate layout composite figure composite figure point2 d anchor point2 d /** * which lays out a figure it is called by the figure * a layout task is to be performed implementing classes * specify a certain layout algorithm in * * @param anchor start point the layout * @param minimum point the layout */ rectangle2 d layout composite figure composite figure point2 d anchor point2 d } 