/* * @ # label figure 2 0 2006 01 14 * * copyright c 1996 2006 by the original authors of * and all its contributors * all rights reserved * * software is the confidential and proprietary information of * confidential information you shall not disclose * such confidential information and shall use it only in accordance * with the terms of the license agreement you entered into with * */package draw * font * geom * * io * /** * label figure * * @author werner randelshofer * @version 2 0 2006 01 14 changed to support coordinates * <br>1 0 8 march 2004 created */public label figure text figure figure listener { text holder target /** creates a instance */ label figure { label } label figure text { set text text set editable } set label text holder target { target != { target remove figure listener } target = target target != { target add figure listener } } text holder get label { target == ? target } /** * a specialized tool the given coordinate * <p> no specialized tool is available */ tool get tool point2 d p { target != contains p ? text tool target } figure area invalidated figure event e { } figure attribute changed figure event e { } figure added figure event e { } figure changed figure event e { } figure removed figure event e { e get figure == target { target remove figure listener target = } } figure request remove figure event e { } remap hash map old to { remap old to target != { figure target = figure old to get target target != { target remove figure listener target = text holder target target add figure listener } } }} 