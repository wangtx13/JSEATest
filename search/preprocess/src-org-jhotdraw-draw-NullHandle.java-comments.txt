/* * @ # handle 1 0 2003 12 01 * * copyright c 1996 2006 by the original authors of * and all its contributors * all rights reserved * * software is the confidential and proprietary information of * confidential information you shall not disclose * such confidential information and shall use it only in accordance * with the terms of the license agreement you entered into with * ??? */package draw * * /** * a handle that doesn't change the owned figure its only purpose is * to show feedback that a figure is selected * * @author werner randelshofer * @version 1 0 2003 12 01 derived from 5 4b1 */public handle locator handle { /** creates a instance */ handle figure owner locator locator { owner locator } cursor get cursor { cursor get cursor } track start point anchor modifiers ex { } track step point anchor point modifiers ex { } track end point anchor point modifiers ex { } /** * creates handles each of a * figure and adds them to the provided collection */ add handles figure f collection< handle> handles { handles add handle f relative locator 0f 0f handles add handle f relative locator 0f 1f handles add handle f relative locator 1f 0f handles add handle f relative locator 1f 1f } /** * draws handle * handles are drawn as unfilled rectangles */ draw graphics2 d g { rectangle r = get bounds g set color color white g draw rect r x + 1 r y + 1 r width 3 r height 3 g set stroke basic stroke g set color color black g draw rect r x r y r width 1 r height 1 } } 