/* * @ # composite transform edit 1 0 2006 01 21 * * copyright c 1996 2006 by the original authors of * and all its contributors * all rights reserved * * software is the confidential and proprietary information of * confidential information you shall not disclose * such confidential information and shall use it only in accordance * with the terms of the license agreement you entered into with * */package draw undo * geom * /** * composite transform edit * * @author werner randelshofer * @version 1 0 2006 01 21 created */public composite transform edit undoable edit { figure owner affine transform tx /** * edit has never received <code>end</code> */ in progress /** creates a instance */ composite transform edit figure owner affine transform tx { owner = owner tx = affine transform tx clone in progress = } get presentation name { figur transformieren } add edit undoable edit an edit { an edit == { end } { ! in progress { } { an edit die } } } replace edit undoable edit an edit { /* an edit composite transform edit { composite transform edit that = composite transform edit an edit that owner == owner { tx concatenate that tx that die } }*/ } redo cannot redo { redo owner will change owner basic transform tx owner changed } undo cannot undo { undo owner will change { owner basic transform tx create inverse } noninvertible transform ex { ex print stack trace } owner changed } /** * edit is in progress that is it has not * received end generally means that edits are still being * added to it * * @see	#end */ is in progress {	return in progress } /** * sets <code>in progress</code> to * * @see #can undo * @see #can redo */ end { in progress = } /** * <code>is in progress</code> or * * * @see	#is in progress */ can undo { !is in progress can undo } /** * <code>is in progress</code> or * * * @see	#is in progress */ can redo { !is in progress can redo }} 